<div class="row">
  <% if genre.errors.any? %>
		<div class="alert alert-danger text-center">
			<% genre.errors.full_messages.each do |message| %>
				<%= message %>
			<% end %>
		</div>
  <% end %>
</div>